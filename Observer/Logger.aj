import java.io.File;
import java.io.FileWriter;
import java.io.IOException;
import java.util.Calendar;
import java.util.Date;
import colores.GamaColores;

/*public aspect Logger {
	declare parents: ColorButton implements Subject
	declare parents: ConsoleLog implements Observer 
    declare parents: ScreenBackGround implements Observer
    pointcut stateChanges(): call(void notify(GamaColores color));
    after(): stateChanges(){
		System.out.println("abc");

    }	
    
}*/
